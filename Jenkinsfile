pipeline {
    
    stages {
        
        stage('Print All') {
            steps {
                sh 'echo "Showing all directories!" '
            }

            steps {
                sh 'ls -ah'
            }
        }
    }
}